# Fungible configuration.
[user]
	name = Damien Sorresso
	email = dsorresso@microsoft.com

[url "git@github.com:fungible-inc/"]
	insteadOf = fungible://

# git-fix specifications for Fungible at Microsoft. Issues are tracked with
# Jira, which we don't yet have a command line interface for.
[fix "branch"]
	userName = dsorresso
	wipNamespace = wip
	prNamespace = PR
	commitTrailerProperty = Bug-Id
	initDone = true

[fix "problem"]
	regex = [0-9]{5,}
	issueTrackerCommand =
	getVerb =
	setVerb =
	stageProperty =
	titleProperty =
	branchProperty =
